name: Firmware CI

on:
  push:
    branches: [main]
    paths:
      - '*.ino'
      - 'platformio.ini'
      - 'src/**'
      - 'include/**'
  pull_request:
    branches: [main]
    paths:
      - '*.ino'
      - 'platformio.ini'
      - 'src/**'
      - 'include/**'

jobs:
  build:
    name: Build Firmware
    runs-on: ubuntu-latest
    strategy:
      matrix:
        platform:
          - espressif32:esp32

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install PlatformIO
        run: |
          python -m pip install --upgrade pip
          pip install platformio

      - name: Cache PlatformIO packages
        uses: actions/cache@v4
        with:
          path: |
            ~/.platformio/packages
            ~/.platformio/platforms
          key: ${{ runner.os }}-pio-${{ hashFiles('platformio.ini') }}
          restore-keys: |
            ${{ runner.os }}-pio-

      - name: Build firmware
        run: pio run

      - name: Check firmware size
        run: |
          echo "=== Firmware Size Report ==="
          pio run --target size || true

  lint:
    name: Lint & Static Analysis
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install PlatformIO
        run: |
          python -m pip install --upgrade pip
          pip install platformio

      - name: Check for common issues
        run: |
          echo "=== Checking .ino file structure ==="
          if [ -f "AegisBeacon.ino" ]; then
            echo "Main .ino file found"
            # Check for required includes
            for lib in RadioLib U8g2lib TinyGPS++; do
              if grep -q "#include.*${lib}" AegisBeacon.ino; then
                echo "OK: ${lib} included"
              else
                echo "WARNING: ${lib} not found in includes"
              fi
            done
            # Check for serial output
            if grep -q "Serial.begin" AegisBeacon.ino; then
              echo "OK: Serial initialized"
            else
              echo "WARNING: Serial.begin() not found"
            fi
            # Check for watchdog
            if grep -q "esp_task_wdt\|wdt_enable\|yield()" AegisBeacon.ino; then
              echo "OK: Watchdog or yield found"
            else
              echo "INFO: No explicit watchdog calls found"
            fi
            # Check for deep sleep
            if grep -q "esp_deep_sleep_start\|deepSleep" AegisBeacon.ino; then
              echo "OK: Deep sleep implementation found"
            else
              echo "WARNING: Deep sleep not found"
            fi
            # Check for WiFi/BT disable in beacon mode
            if grep -q "WiFi.begin\|btStop" AegisBeacon.ino; then
              echo "OK: WiFi/BT management found"
            else
              echo "INFO: No WiFi/BT management found"
            fi
            # Check for GPS integration
            if grep -q "TinyGPS\|GPS" AegisBeacon.ino; then
              echo "OK: GPS integration found"
            else
              echo "INFO: No GPS code found"
            fi
            # Check for frequency defines
            if grep -q "DEFAULT_FREQ\|FREQ" AegisBeacon.ino; then
              echo "OK: Frequency configuration found"
            else
              echo "WARNING: No frequency configuration found"
            fi
            # Check for Morse timing
            if grep -q "WPM\|dot\|dash\|morse" AegisBeacon.ino; then
              echo "OK: Morse timing found"
            else
              echo "WARNING: No Morse timing found"
            fi
            # Check for OLED display
            if grep -q "U8g2\|oled\|OLED\|display" AegisBeacon.ino; then
              echo "OK: Display code found"
            else
              echo "INFO: No display code found"
            fi
            # Check for audio output
            if grep -q "DAC\|tone\|audio\|AUDIO" AegisBeacon.ino; then
              echo "OK: Audio output found"
            else
              echo "INFO: No audio code found"
            fi
            # Check for battery monitoring
            if grep -q "ADC\|battery\|BATTERY\|VBAT" AegisBeacon.ino; then
              echo "OK: Battery monitoring found"
            else
              echo "INFO: No battery monitoring found"
            fi
            # Check for config portal
            if grep -q "WiFi.softAP\|captive\|portal" AegisBeacon.ino; then
              echo "OK: Config portal found"
            else
              echo "INFO: No config portal found"
            fi
            # Check for LED indicators
            if grep -q "LED\|led\|digitalWrite.*2\|digitalWrite.*4" AegisBeacon.ino; then
              echo "OK: LED indicators found"
            else
              echo "INFO: No LED code found"
            fi
          else
            echo "WARNING: AegisBeacon.ino not found in root"
          fi

      - name: Validate platformio.ini
        run: |
          if [ -f "platformio.ini" ]; then
            echo "=== platformio.ini contents ==="
            cat platformio.ini
            echo ""
            echo "=== Checking required fields ==="
            for field in board platform framework lib_deps monitor_speed; do
              if grep -q "^${field}" platformio.ini; then
                echo "OK: ${field} configured"
              else
                echo "WARNING: ${field} not found"
              fi
            done
          else
            echo "ERROR: platformio.ini not found"
            exit 1
          fi

  assign-reviewer:
    name: Assign Reviewer
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    permissions:
      pull-requests: write
    steps:
      - name: Assign Leo-Galli as reviewer
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.pulls.requestReviewers({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.issue.number,
              reviewers: ['Leo-Galli']
            });
            console.log('Assigned Leo-Galli as reviewer');
